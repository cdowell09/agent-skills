# Domain Docs

This repository uses a single domain context.

Before working, read:

- `CONTEXT.md`
- Relevant decisions under `docs/adr/`

Use the glossary's canonical vocabulary. Surface conflicts with existing ADRs rather than silently overriding them. Missing domain files are created lazily when terminology or architectural decisions are resolved.
