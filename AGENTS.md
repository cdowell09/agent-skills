## Agent skills

### Issue tracker

Issues and PRDs live in GitHub Issues for `cdowell09/agent-skills`. Use the `gh` CLI by default and the GitHub plugin when CLI coverage or authentication is unavailable. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the repository's canonical triage-role labels. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository. Read `CONTEXT.md` and relevant ADRs before working. See `docs/agents/domain.md`.
